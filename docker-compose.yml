services:
  bt_dev:
    build: .
    container_name: bt_dev_container
    volumes:
      - .:/ros2_ws
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
    network_mode: host
    privileged: true
    stdin_open: true
    tty: true
